#include <iostream>
using namespace std;
const int MAX = 1001;
int t, n, m;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    cin >> t;
    for (int i = 0; i < t; i++) {
        cin >> n >> m;
        for (int j = 0; j < m; j++) {
            int a, b;
            cin >> a >> b;
            
        }
        cout << n-1 << '\n';
    }
    
    return 0;
}
